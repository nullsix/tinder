<% title = "'#{@piece.title}' - versions" %>
<% provide :title, title %>
<% piece_bar = render partial: "pieces/piece_bar", locals: { piece: @piece, current_page: :versions } %>

<%= piece_bar %>
<div class="row">
  <div class="span12">
    <table class="versions table table-striped">
      <% @versions.each do |version| %>
        <% last_modified = "Last modified #{time_ago_in_words(version.updated_at)} ago" %>
        <tr class="version-row">
          <td>
            <p class="version-title">
              <%= link_to version.title, piece_version_path(piece_id: @piece.id, id: version.number) %> 
              <small class="version-number muted">
                (version #<%= version.number %>)
              </small>
            </p>
            <small class="version-last-modified muted"><%= last_modified %></small>
          </td>
        </tr>
      <% end %>
    </table>
  </div>
</div>
<%= piece_bar %>
